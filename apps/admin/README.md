# WortMeister Admin Portal

React/Vite admin dashboard for operations, analytics, audit logs, and activity timeline.

```bash
npm install
npm run dev
```

Set:

```bash
VITE_ADMIN_API_URL=http://localhost:8080/api/admin/v1
```

Paste an admin JWT into the portal to access protected admin APIs.
