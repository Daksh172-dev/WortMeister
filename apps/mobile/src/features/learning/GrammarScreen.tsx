import { FeatureScreen } from "@/shared/components/FeatureScreen";

export function GrammarScreen() {
  return <FeatureScreen title="Grammar" eyebrow="Rules" subtitle="Readable grammar topics with practical examples." endpoint="/learning/grammar" searchable />;
}
