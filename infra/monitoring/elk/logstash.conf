input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

filter {
  mutate {
    add_field => { "platform" => "wortmeister" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "wortmeister-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
